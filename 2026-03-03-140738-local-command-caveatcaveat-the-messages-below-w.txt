
  Project Review — Round 3

  8. Accessor visibility in DocumentFile.php:45,53

  url() and icon() are public; should be protected to match User and UserBadge
  conventions.

  9. Duplicate badge-resolution block in ProfileController (lines 23-27 and
  47-52)

  Same 4-line block duplicated — extract to a private method.

  10. Magic 'admin' string in DocumentPolicy:12 and CommentPolicy:12

  No enum/constant is used for role comparison.

  11. Subject.php:18 — possibly stale schoolType() BelongsTo alongside
  schoolTypes() BelongsToMany

  If the relationship is actually many-to-many, the singular schoolType() and
  the forSchoolType scope (which filters by school_type_id column) may be
  incorrect.

  12. User.php:121 — return type float|int should just be int

  14. Inline SVGs in badge-grid.blade.php, badge-progress.blade.php,
  badge-icon.blade.php

  Several icon paths are inlined instead of using <x-icon-*> components.

  15. Hardcoded URL in document-row.blade.php:110

  url('/dodajanje?uredi=...') — should use route().

  16. Empty filters([]) in 8 Filament resources + empty getRelations() in
  UserResource

  17. bootstrap/app.php empty withMiddleware and withExceptions callbacks

  Pure scaffolding noise — can be removed.

  18. Redundant Fortify::redirectUserForTwoFactorAuthenticationUsing()
  (FortifyServiceProvider:36)

  Re-registers the default — no-op.

  ---
  Low Priority

  - BrowseMeilisearchQueryBuilder.php:19 — redundant
  array_values(array_keys(...))
  - BadgeService.php:28-42,93-98 — milestone thresholds duplicated in two
  methods
  - BadgeRegistry.php and SchoolTypeUiConfig.php — could be final
  - User.php:133-136 — redundant (int) cast
  - FortifyServiceProvider — generic scaffolding PHPDoc comments
  - routes/console.php — empty file
  - DownloadedDocumentsTab — unbounded pluck('document_id') could be replaced
  with a join
  - Three dead SFC stub files (⚡uploaded-documents-tab,
  ⚡downloaded-documents-tab, ⚡saved-documents-tab in
  resources/views/components/)
  - Unused $accentColor prop in document-row.blade.php
  - Slovenian plural logic scattered across 3 views
  - SchoolTypeUiConfig::all() called per <x-document-row> render instead of once
   in parent

✻ Brewed for 3m 55s

❯ /export
  ⎿  Conversation exported to:
     2026-03-03-140658-local-command-caveatcaveat-the-messages-below-w.txt

❯ /export
  ⎿  Conversation exported to:
     2026-03-03-140706-local-command-caveatcaveat-the-messages-below-w.txt
